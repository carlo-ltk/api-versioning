name: deploy

on:
  release:
    types: [published]

jobs:

  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      
      - name: Set Version variables
        id: version
        run: | 
          echo ${{ github.ref }} | sed -e "s/^refs\/tags\///"
          echo TAG=$(echo ${{ github.ref }} | sed -e "s/^refs\/tags\///") >> $GITHUB_OUTPUT
          echo COMMIT=${{ github.sha }} >> $GITHUB_OUTPUT

      - name: Output Version variables
        run: |
          echo "${{ steps.version.outputs.TAG }}"
          echo "${{ steps.version.outputs.COMMIT }}"

      - name: Checkout
        uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1.7.0
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Update versions SSM param
        run: |
          aws ssm get-parameter --name fnd-api-s2-versions --query "Parameter.Value" --output text | jq '.  += [ { tag: ${{ steps.version.outputs.TAG }}, commit: ${{ steps.version.outputs.COMMIT }} }]

      #- name: Serverless Deploy ${{ steps.version.outputs.COMMIT }}
      #  run: | 
      #    npm run serverless deploy -- --stage ${{ steps.version.outputs.COMMIT }}

      - name: Configure Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.3.0

      - name: Terraform output 
        run: |
          npm run terraform -- init
          npm run terraform -- output

      #- name: AWS get SSM param and run JQ
      #  run: |
      #    aws ssm get-parameter --name fnd-api-s2-versions --query "Parameter.Value" --output text | jq '.[]'

      #- name: Parsing test
      #  run: echo ${{ github.ref }} | sed -e "s/^refs\/tags\/v//" | sed -r "s/\./d/g"

      